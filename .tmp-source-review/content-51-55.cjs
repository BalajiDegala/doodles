const {topic}=require('./new40-content.cjs');
topic(51,{
summary:['GitOps records desired configuration in a versioned source.','A controller reconciles selected source content into a cluster.','Argo CD and Flux provide different resources and workflows for that reconciliation.','Drift correction and deletion depend on explicit controller policy.'],cue:'Commit intent; reconcile deliberately.',plain:'Maya keeps an approved opening plan in a shared ledger. An operator compares the actual counter arrangement with that plan and follows agreed rules for making changes.',story:'The Bookshop’s authored manifests could become a GitOps source. We map the path, revision, destination, permissions, and recovery steps before connecting any repository.',
detail:'Argo CD Applications describe source and destination; automated synchronization, pruning, and self-healing are separate settings. Flux composes source and reconciliation controllers; its Kustomization custom resource is different from a local kustomization.yaml file.\n\nA Git revert restores desired configuration, not database history or external side effects. Manual emergency changes may be reconciled away, so incident procedures must coordinate controller state and the source. Repository history alone is not a complete runtime audit trail.',source:'[Argo CD automated sync](https://argo-cd.readthedocs.io/en/stable/user-guide/auto_sync/), [Flux Kustomization](https://fluxcd.io/flux/components/kustomize/kustomizations/)',
guide:'Read the [chapter manifests](../../manifest-guide.md) and the earlier [Kustomize example](../../../04-bookshop-operations/topics/35-kustomize-and-helm/manifest-guide.md). No Application or Flux resource is installed here.\n\nAn actual source definition needs a real repository URL, revision, and path. A destination needs an explicitly permitted cluster and namespace. Argo CD’s §syncPolicy§ or Flux’s §prune§/§suspend§ settings affect changes and deletion. A controller’s ServiceAccount and platform configuration determine what it can apply.\n\nOur local four-file base is configuration content, not a connected GitOps deployment. Do not create a resource with a fabricated repository URL or assume that a namespace automatically grants controller access.',
runbook:'## Prerequisites\n\nLocal files are sufficient for the design exercise. Inspect existing controller resources only with permission.\n\n~~~powershell\nkubectl api-resources --api-group=argoproj.io\nkubectl api-resources --api-group=kustomize.toolkit.fluxcd.io\nkubectl -n k8s-learning-platform get deployment platform-catalog -o yaml\n~~~\n\nMissing custom APIs mean no live GitOps exercise is available.\n\n## Write the reconciliation contract\n\nRecord the intended repository/path/revision, destination context/namespace, allowed resource types, review process, drift policy, prune policy, and recovery owner. Explain what happens if someone manually changes replicas. Compare an intentional Git change with an unauthorized drift correction.\n\nIf a pre-existing test Application/Kustomization is provided, read its observed revision, readiness/sync status, and events. Do not repoint it at these files or enable automation as part of this question.\n\n## Troubleshooting and cleanup\n\nSeparate source authentication, render errors, destination permission, and unhealthy workloads. Discovery alone does not prove reconciliation. No repository or controller configuration is changed, so nothing needs cleanup.'});
topic(52,{
summary:['External Secrets Operator maps an external source into Kubernetes Secrets.','A SecretStore describes a provider and its access configuration.','An ExternalSecret selects keys, refresh behaviour, and a target Secret.','Source synchronization does not automatically reload every consuming process.'],cue:'Store connects; mapping selects; target delivers.',plain:'Maya’s approved vault keeps the source record. A clerk copies only the required item into the counter’s locked drawer, and periodically checks whether it changed.',story:'An optional fake provider returns a public training value. The operator creates a target Secret, demonstrating reconciliation without accessing a real vault or credential.',
detail:'Real providers require narrowly scoped identity and network access. Once copied, values also exist in Kubernetes Secrets and are subject to that cluster’s RBAC, encryption, audit, and backup controls. A controller outage or provider failure may leave stale values; inspect conditions and refresh times.\n\nThe fake provider proves mapping and target creation only. It does not prove cloud authentication, vault availability, secret rotation, or application reload. Never put real credentials into the fake provider’s inline data.',source:'[ExternalSecret API](https://external-secrets.io/latest/api/externalsecret/), [fake provider](https://external-secrets.io/latest/provider/fake/)',
guide:'Files: [fake SecretStore](optional/10-fake-store.yaml) and [ExternalSecret](optional/20-external-secret.yaml). Both require served §external-secrets.io/v1§ APIs.\n\nThe namespaced Store §bookshop-fake§ maps key §/bookshop/practice§, version §v1§, to §not-a-real-password§. The ExternalSecret’s §remoteRef§ matches both key and version, and §secretKey: practice§ names the destination key. §refreshInterval: 1m§ requests periodic checks.\n\n§target.name: bookshop-imported§ names the generated Secret; §creationPolicy: Owner§ asks the operator to own it. No application Pod consumes this value, keeping source synchronization separate from application configuration delivery.',
runbook:'## Prerequisites\n\nExisting ESO installation serving v1, fake-provider support, and permission to create namespaced Store/ExternalSecret objects. If absent, read the YAML and skip application.\n\n~~~powershell\nkubectl api-resources --api-group=external-secrets.io\nkubectl apply --dry-run=server -f topics/52-external-secrets/optional/\nkubectl apply -f topics/52-external-secrets/optional/\nkubectl -n k8s-learning-platform wait --for=condition=Ready externalsecret/bookshop-import --timeout=120s\nkubectl -n k8s-learning-platform describe externalsecret bookshop-import\n$imported = kubectl -n k8s-learning-platform get secret bookshop-imported -o json | ConvertFrom-Json\n[Text.Encoding]::UTF8.GetString([Convert]::FromBase64String($imported.data.practice)) -eq \'not-a-real-password\'\n~~~\n\nExpect Ready and True. The comparison checks only the public dummy value and prints a boolean. Inspect status/conditions if the Store, version, or key cannot be resolved.\n\n## Cleanup\n\nRun only when you created these resources:\n\n~~~powershell\nkubectl delete -f topics/52-external-secrets/optional/ --ignore-not-found\nkubectl -n k8s-learning-platform delete secret bookshop-imported --ignore-not-found\n~~~\n\nThis does not modify an external secret manager.'});
topic(53,{
summary:['IfNotPresent can use a locally available image.','Always resolves the requested image through the registry but can reuse cached content.','Never requires the image to be present on the chosen node.','Private image access needs appropriate registry credentials or a configured credential provider.'],cue:'Resolve identity, reuse layers, authenticate separately.',plain:'Maya can use a stocked manual, check the supplier for the current edition, or insist on local stock. Having a manual on one shelf does not put it on every shelf.',story:'Two small workers explicitly use IfNotPresent and Always for the same BusyBox tag. We inspect the resulting image IDs and events without creating a bogus registry login.',
detail:'A tag can move; a digest identifies particular image content. Default pull policy is assigned when the object is created and does not automatically track later tag changes. Always does not mean redownloading every cached layer.\n\nAn imagePullSecret must be usable in the Pod’s namespace. Some platforms use kubelet credential providers or workload/node identities for registries. Do not put live tokens into commands, Git, or documentation; use the organization’s existing credential flow. Image cache alone is not authorization proof.',source:'[Kubernetes images](https://kubernetes.io/docs/concepts/containers/images/), [private registry image pulls](https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/)',
guide:'Files: [cached worker](manifests/pull-cached.yaml) and [registry-resolving worker](manifests/pull-resolve.yaml).\n\nBoth use §busybox:1.36§, identical commands, and common restrictions. Their explicit §imagePullPolicy§ differs. Kubernetes reports runtime-resolved content under §status.containerStatuses[].imageID§; the authored tag and actual image ID answer different questions.\n\nNo imagePullSecrets entry is needed for this public image. For a real private image, §spec.imagePullSecrets[].name§ refers to an existing appropriate Secret in this namespace; it does not contain the password itself. This exercise makes no changes to the default ServiceAccount.',
runbook:'## Prerequisites\n\nBase namespace and registry access. Always needs registry resolution even when content is cached.\n\n~~~powershell\nkubectl apply -f topics/53-image-pulls/manifests/\nkubectl -n k8s-learning-platform wait --for=condition=Ready pod/pull-cached pod/pull-resolve --timeout=120s\nkubectl -n k8s-learning-platform get pod pull-cached pull-resolve -o yaml\nkubectl -n k8s-learning-platform describe pod pull-resolve\n~~~\n\nCompare requested image, explicit pull policy, imageID, and pull events. Equal IDs are expected if the tag resolves to the same content; they do not prove that Always skipped registry access. Cache messages do not guarantee behaviour on another node.\n\n## Troubleshooting and cleanup\n\nDistinguish missing tags, authentication denial, registry/network failure, and platform admission. Do not print registry Secret contents while diagnosing. An unavailable registry is a dependency failure for this test.\n\n~~~powershell\nkubectl delete -f topics/53-image-pulls/manifests/ --ignore-not-found\n~~~'});
topic(54,{
summary:['CoreDNS answers cluster DNS queries using configured plugins.','Forwarding can send selected DNS zones to another resolver.','Custom host mappings and caching change how names are answered.','Editing the shared DNS configuration can affect many workloads.'],cue:'Match the zone, choose the resolver, watch the cache.',plain:'Maya’s directory desk answers local counter names and forwards supplier questions to another directory. A wrong forwarding address can break every caller using that rule.',story:'We inspect the catalog’s resolver configuration and read a deliberately non-deployable Corefile example. The shared cluster DNS configuration is observed only.',
detail:'A domain-specific server block can forward corporate zones while other queries keep their normal path. Forwarding requires reachable upstream resolvers and a design that avoids loops. DNS caching and application caching can delay visible changes.\n\nProvider-managed clusters may expose customization through a supported extension rather than direct ConfigMap edits. Read the platform’s instructions before proposing a change. Query logging has volume and data-exposure costs; it should be deliberately scoped and time-limited.',source:'[Customizing DNS](https://kubernetes.io/docs/tasks/administer-cluster/dns-custom-nameservers/), [CoreDNS forward plugin](https://coredns.io/plugins/forward/)',
guide:'Reference: [Corefile](reference/Corefile). It is CoreDNS server configuration, not YAML.\n\n§.:1053§ listens for the root DNS zone on port 1053. §errors§ reports failures. Health/readiness endpoints use separate ports. §hosts§ maps the documentation name §supplier.bookshop.test§ to documentation address §192.0.2.10§. §fallthrough§ allows unanswered names to continue through the chain. §forward . 192.0.2.53§ shows an upstream, and §cache 30§ shows a cache setting.\n\nThe documentation addresses are intentionally not a working DNS service. Do not replace the cluster’s real Corefile with this example. It omits the normal Kubernetes plugin and production configuration.',
runbook:'## Prerequisites\n\nBase Pod access for DNS checks. Reading the system CoreDNS ConfigMap requires extra permission and the expected ConfigMap name for the distribution.\n\n~~~powershell\nkubectl -n k8s-learning-platform exec deployment/platform-catalog -- cat /etc/resolv.conf\nkubectl -n k8s-learning-platform exec deployment/platform-catalog -- nslookup platform-catalog.k8s-learning-platform.svc\nkubectl -n kube-system get configmap coredns -o yaml\n~~~\n\nExpect the catalog name to resolve through the Pod’s configured resolver. If the ConfigMap is absent or forbidden, obtain the provider’s DNS configuration evidence instead. Do not treat its absence as proof that DNS is broken.\n\n## Design proof and cleanup\n\nAnnotate the reference with which block handles a local host entry, an unknown name, and an upstream timeout. Propose a real corporate-zone forwarding change with an owner, test names, and rollback plan; do not apply it. No DNS configuration changes occur, so no cleanup is needed.'});
topic(55,{
summary:['EndpointSlices group Service endpoint information into smaller objects.','Their addresses, ports, and conditions describe available backends.','Controllers maintain slices for ordinary selector-based Services.','Consumers should use supported discovery APIs instead of relying on legacy Endpoints behaviour.'],cue:'A destination directory divided into manageable pages.',plain:'Maya splits a long counter directory into pages. Changing one counter updates the relevant page instead of replacing the entire directory for every reader.',story:'We compare the catalog’s ready Pods with the EndpointSlices generated for its Service. No thousands-of-Pods load test is needed to understand the structure.',
detail:'EndpointSlice reached stable discovery.k8s.io/v1 in Kubernetes 1.21. The legacy Endpoints API is deprecated from 1.33; deprecation does not mean immediate removal. At large endpoint counts it also has truncation limitations.\n\nConsumers must account for readiness, termination, address family, multiple slices, and transient updates. A Service may have more than one slice; assuming that the first returned object is the complete backend list creates bugs. Avoid editing controller-managed slices as a way to repair selectors.',source:'[EndpointSlices](https://kubernetes.io/docs/concepts/services-networking/endpoint-slices/), [Endpoints deprecation](https://kubernetes.io/blog/2025/04/24/endpoints-deprecation/)',
guide:'Read the [Service](../../manifests/30-service.yaml); no manually authored EndpointSlice is needed.\n\nIts selector identifies §app: platform-catalog§. Generated slices carry §kubernetes.io/service-name: platform-catalog§. §addressType§ identifies IPv4/IPv6 or another supported family, §ports§ reports receiving ports, and each endpoint includes addresses and conditions. §targetRef§ can identify the backing Pod.\n\nThe slice’s actual port is the Pod listener, 8080, while the Service exposes 80. Read §conditions.ready§ along with termination/serving information where present; the absence of one optional field is not itself an error.',
runbook:'## Prerequisites\n\nReady base replicas and read permission for EndpointSlices.\n\n~~~powershell\nkubectl -n k8s-learning-platform get pods -l app=platform-catalog -o wide\nkubectl -n k8s-learning-platform get endpointslices -l kubernetes.io/service-name=platform-catalog\nkubectl -n k8s-learning-platform get endpointslices -l kubernetes.io/service-name=platform-catalog -o yaml\n~~~\n\nMatch endpoint addresses and targetRef names to the ready Pods. Inspect all returned slices. Expect port 8080, appropriate address families, and ready endpoints once the rollout settles.\n\n## Troubleshooting and cleanup\n\nAn empty directory needs selector/readiness/controller checks. A transient terminating endpoint needs condition-aware interpretation. Do not hand-edit generated slices. Compare the API group/version with any old controller code that still reads Endpoints.\n\nNo resources are created by these reads, so nothing needs removal.'});
