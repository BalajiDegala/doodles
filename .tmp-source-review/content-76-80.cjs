const {topic}=require('./new40-content.cjs');
topic(76,{
summary:['Audit records describe Kubernetes API activity and its caller context.','Audit policy selects event scope and detail using ordered rules.','Request and response bodies can expose sensitive values.','Ordinary Kubernetes Events and application logs are not substitutes for API audit records.'],cue:'Who requested what, when, and with which result?',plain:'Maya’s office records who changed a counter plan. A worker’s error note or a customer receipt answers a different question.',story:'We make one harmless catalog read and define the evidence an administrator would correlate in an existing audit backend. A reference policy illustrates Metadata-level logging without enabling it.',
detail:'Audit levels include None, Metadata, Request, and RequestResponse. The first matching rule controls detail. Sensitive request bodies, including Secret writes and other credential-bearing objects, can be exposed by body-level logging.\n\nAudit collection also needs a configured backend, retention, protected access, and a decision about failure handling. Managed services may expose audit logs through provider settings rather than editable API server files. A namespace’s Event list is useful operational evidence but is not the audit history of every API call.',source:'[Kubernetes auditing](https://kubernetes.io/docs/tasks/debug/debug-cluster/audit/)',
guide:'Reference: [audit policy](reference/audit-policy.yaml). §audit.k8s.io/v1§ Policy is API-server configuration, not a namespaced resource for kubectl apply.\n\nThe first rule records core Secret operations at Metadata level across namespaces, so it records no bodies. The second records reliability-namespace activity at Metadata level. The final None rule omits everything else. Rule order therefore matters.\n\nThis deliberately narrow reference is not a complete organization audit policy. It neither activates a backend nor changes current cluster settings. Review actual sensitivity, coverage, volume, retention, and provider support before any future configuration change.',
runbook:'## Prerequisites\n\nBase read access. Correlation requires an existing audit backend and an authorized reader.\n\n~~~powershell\nGet-Date -Format o\nkubectl auth whoami\nkubectl -n k8s-learning-reliability get deployment reliable-catalog\nGet-Date -Format o\n~~~\n\nRecord the time window, effective identity, verb get, resource deployments, namespace, and name. Do not export credentials. Ask the authorized audit reader to find the matching request/result and explain its stage and detail level.\n\n## Expected result and limits\n\nA matching audit record proves configured collection for that request. If unavailable, mark audit verification skipped; do not substitute kubectl get events or a container log.\n\n## Cleanup\n\nNo audit policy or backend is installed. The read request may leave an audit record according to existing policy; no application resource needs removal.'});
topic(77,{
summary:['Base64 encoding of Secret API data is not encryption.','Encryption at rest protects the stored representation when properly configured.','API-authorized readers still receive usable Secret values.','Key management, rotation, old-data rewriting, and recovery are separate operational requirements.'],cue:'Encoding is packaging; encryption needs keys and a recovery plan.',plain:'Maya can disguise a note’s alphabet or lock it in a safe. The alphabet is reversible packaging; the safe depends on protected keys and authorized access.',story:'A Secret contains an explicitly public training value. We verify its API round trip and explain why that proves neither encryption nor lack of encryption in the datastore.',
detail:'Standard Kubernetes does not automatically make every underlying Secret record encrypted through an encryption provider; distributions and managed services can configure additional protection. Encryption is transparent to ordinary authorized API reads, so decoding a returned value cannot diagnose storage protection.\n\nKMS v2 is the modern Kubernetes envelope-encryption integration; actual support and key ownership depend on the platform. Provider ordering, key availability, migration of previously stored records, and restoration all matter. Do not rewrite all cluster Secrets or edit control-plane encryption configuration as a namespace exercise.',source:'[Encrypting data at rest](https://kubernetes.io/docs/tasks/administer-cluster/encrypt-data/), [KMS provider](https://kubernetes.io/docs/tasks/administer-cluster/kms-provider/)',
guide:'File: [public dummy Secret](manifests/10-practice-secret.yaml). §type: Opaque§ is a general Secret type. §stringData.note: public-training-value§ is authoring convenience; the API returns the value under base64-encoded §data.note§.\n\nNo real password, encryption key, KMS endpoint, or EncryptionConfiguration appears in this file. The name §encryption-practice§ labels the lesson rather than enabling encryption. An encryption provider is configured at the platform/control-plane layer and cannot be inferred from this Secret manifest.',
runbook:'## Prerequisites\n\nPermission to create/read/delete this disposable Secret. Read only the known public training object, not unrelated Secrets.\n\n~~~powershell\nkubectl apply -f topics/77-secret-encryption/manifests/\n$trainingSecret = kubectl -n k8s-learning-reliability get secret encryption-practice -o json | ConvertFrom-Json\n[Text.Encoding]::UTF8.GetString([Convert]::FromBase64String($trainingSecret.data.note)) -eq \'public-training-value\'\n~~~\n\nExpect True. This verifies the API’s encoding round trip, not the at-rest representation.\n\n## Platform evidence\n\nRecord the responsible owner, actual encryption configuration/provider, key-access controls, rotation/migration procedure, and restore evidence. Obtain datastore verification only through the authorized platform procedure. A readable API value or a base64 string is not proof either way.\n\n## Cleanup\n\n~~~powershell\nkubectl delete -f topics/77-secret-encryption/manifests/ --ignore-not-found\n~~~'});
topic(78,{
summary:['RuntimeClass selects a configured runtime handler for a Pod.','The handler must exist on eligible nodes and be supported by the runtime integration.','Sandboxed runtimes can strengthen isolation with compatibility and resource tradeoffs.','Declared overhead and scheduling constraints affect placement and accounting.'],cue:'Choose an installed enclosure, then account for its cost.',plain:'Maya can place certain tasks in an ordinary room or a stronger enclosure. Naming an enclosure does not build it, and its walls take up space.',story:'We inspect the available RuntimeClasses and the catalog’s default runtime choice. No host runtime or virtualization component is installed.',
detail:'gVisor interposes a userspace kernel interface for supported workloads; Kata Containers commonly uses lightweight virtual machines. These designs reduce some shared-kernel risks, but neither is an unconditional guarantee against escape or every attack.\n\nEvaluate syscall/device compatibility, startup time, observability, networking/storage support, resource overhead, and performance for the actual workload. RuntimeClass is a selection mechanism; its name alone does not prove which isolation properties are active on the node.',source:'[RuntimeClass](https://kubernetes.io/docs/concepts/containers/runtime-class/), [gVisor architecture](https://gvisor.dev/docs/architecture_guide/intro/), [Kata Containers](https://katacontainers.io/)',
guide:'Read the [base Deployment](../../manifests/20-deployment.yaml). It omits §runtimeClassName§ and therefore uses the platform’s default runtime handling. This differs from explicitly selecting a configured class.\n\nAn existing RuntimeClass has a cluster-scoped name and §handler§. Its optional §scheduling§ constrains eligible nodes, and §overhead.podFixed§ can add resource accounting beyond the application container requests. The node/runtime integration must implement that handler. No placeholder class or Pod is applied here because an invented handler cannot demonstrate a working sandbox.',
runbook:'## Prerequisites\n\nBase read permission and optional cluster-scoped RuntimeClass read access.\n\n~~~powershell\nkubectl get runtimeclasses\nkubectl -n k8s-learning-reliability get pods -l app=reliable-catalog -o custom-columns=NAME:.metadata.name,RUNTIMECLASS:.spec.runtimeClassName,NODE:.spec.nodeName\n~~~\n\nIf a class exists and is approved for inspection:\n\n~~~powershell\nkubectl get runtimeclass PASTE_CLASS_NAME -o yaml\n~~~\n\nReplace the placeholder with the inspected name. Record handler, overhead, scheduling constraints, supported nodes, and the provider’s isolation evidence. An empty class list means the optional class-selection exercise is unavailable, not that containers cannot run.\n\n## Design result and cleanup\n\nName one workload that might benefit and the compatibility/performance tests it needs. Do not change the base’s runtime class without an approved installed option and a separate test plan. All commands are read-only; no cleanup is needed.'});
topic(79,{
summary:['Progressive delivery advances a release through controlled exposure stages.','Argo Rollouts and Flagger use controllers and policy to manage promotion or rollback.','Traffic providers and analysis configuration determine actual routing and decisions.','Automatic rollback is only as useful as its signals and application recovery behaviour.'],cue:'Expose, observe, decide, then advance.',plain:'Maya tries a new counter with a limited audience, checks agreed results, and expands only when the evidence supports it.',story:'The earlier manual canary becomes the basis for an automated decision plan. We identify the missing controller, routing integration, analysis source, and rollback semantics.',
detail:'Argo Rollouts introduces a Rollout resource; Flagger reconciles its canary workflow around supported workload/traffic-provider integrations. Capabilities differ by controller version and provider. A basic replica-count canary is approximate; precise configured traffic weights need the appropriate routing integration.\n\nUseful analysis needs a baseline, minimum sample, time window, failure handling, and treatment of missing data. “No errors returned by the metrics query” can mean no traffic rather than a good release. Schema/data migrations and external effects still require an application-level recovery strategy.',source:'[Argo Rollouts concepts](https://argo-rollouts.readthedocs.io/en/stable/concepts/), [Flagger canary analysis](https://docs.flagger.app/usage/how-it-works)',
guide:'Reuse the [manual canary guide](../../../03-canary-deployment/manifest-guide.md) and [question 43](../../../05-bookshop-platform/topics/43-release-strategies/lesson.md). They explain the existing stable/canary Deployments and shared Service.\n\nAn automated implementation additionally needs a compatible Rollout or Canary schema, controller, traffic provider, and analysis definition. Those are not present merely because two Deployments exist. No CRD or rollout controller is installed by this question, and a fabricated analysis query would not prove healthy behaviour.',
runbook:'## Prerequisites\n\nAuthored release files are sufficient for the design exercise. Optional controller inspection requires an existing installation and permitted practice objects.\n\n~~~powershell\nkubectl api-resources --api-group=argoproj.io\nkubectl api-resources --api-group=flagger.app\n~~~\n\n## Define the release decision\n\nWrite stages such as 10%, 25%, and full exposure, then attach a meaningful observation window, minimum request count, error/latency threshold, and missing-data decision to each. Identify the traffic provider that can implement the chosen weights. Explain abort, rollback, and incompatible-data handling.\n\nWith an existing test rollout, inspect its phase, analysis results, and routing status using its version-matched runbook. Without one, mark automated promotion untested. The earlier manual canary can still be exercised through its own runbook.\n\n## Cleanup\n\nNo progressive-delivery resource is created here. Clean up any repeated manual canary using its existing procedure.'});
topic(80,{
summary:['Owner references connect dependent API objects to their owners by identity.','Background and foreground deletion differ in how owner/dependent cleanup is observed.','Orphan deletion preserves dependents instead of cascading removal.','Finalizers coordinate cleanup and are not arbitrary shell hooks to remove blindly.'],cue:'Follow the owner chain before closing the counter.',plain:'Maya owns a temporary counter plan, which manages workers. Removing the plan can dismiss the workers, or deliberately leave their manager operating until a separate handover is finished.',story:'We create one disposable Deployment, orphan its ReplicaSet, and verify that the manager/Pod remain. Foreground deletion of that ReplicaSet then removes its managed Pod.',
detail:'A Deployment typically owns ReplicaSets, which own Pods. Names alone are insufficient: ownerReferences include the owner UID. Ownership is not the same as matching labels, and namespaced ownership cannot arbitrarily cross namespaces.\n\nForeground deletion waits for relevant blocking dependents; background deletion lets garbage collection proceed asynchronously. Finalizers can keep deletion pending until their responsible controller completes cleanup. Removing an unknown finalizer can leak external resources or violate recovery assumptions. This exercise adds no custom finalizer.',source:'[Garbage collection](https://kubernetes.io/docs/concepts/architecture/garbage-collection/), [finalizers](https://kubernetes.io/docs/concepts/overview/working-with-objects/finalizers/)',
guide:'File: [disposable Deployment](manifests/10-disposable-deployment.yaml). It has one replica, unique §app: disposable-counter§ selectors, and the shared read-only catalog page. Generated ReplicaSets/Pods receive controller ownership references; the authored Deployment does not hardcode their generated names or UIDs.\n\n§--cascade=orphan§ removes the Deployment while preserving its ReplicaSet. That ReplicaSet still manages its Pod. Deleting the ReplicaSet with §--cascade=foreground§ then waits for its blocking dependents. The shared ConfigMap is referenced as configuration, not owned by this Deployment, so it remains.',
runbook:'## Prerequisites\n\nReady base ConfigMap, room for one small Pod, and permission to create/delete this disposable Deployment and its ReplicaSet. Verify the selector is unused before applying.\n\n~~~powershell\nkubectl -n k8s-learning-reliability get deploy,rs,pods -l app=disposable-counter\nkubectl apply -f topics/80-garbage-collection/manifests/\nkubectl -n k8s-learning-reliability rollout status deployment/disposable-counter --timeout=120s\nkubectl -n k8s-learning-reliability get rs,pods -l app=disposable-counter -o yaml\nkubectl -n k8s-learning-reliability delete deployment disposable-counter --cascade=orphan --wait=true\nkubectl -n k8s-learning-reliability get rs,pods -l app=disposable-counter\n~~~\n\nExpect the ReplicaSet and Pod to remain. Inspect ownerReferences before and after to explain why. Do not recreate the Deployment during the observation, since adoption could change the ownership story.\n\n## Finish cleanup\n\n~~~powershell\nkubectl -n k8s-learning-reliability delete rs -l app=disposable-counter --cascade=foreground --wait=true --timeout=60s\nkubectl -n k8s-learning-reliability get rs,pods -l app=disposable-counter\nkubectl -n k8s-learning-reliability get configmap reliable-catalog-page\n~~~\n\nExpect no exercise ReplicaSet/Pod and the base ConfigMap still present. If interrupted before orphaning, delete the exact disposable Deployment first. Investigate remaining owners/finalizers rather than force-removing unknown finalizers.'});
