const {topic}=require('./new40-content.cjs');
topic(56,{
summary:['etcd is Kubernetes’ standard strongly consistent backing store for API state.','Raft quorum lets a healthy majority agree on updates.','API objects and application volume contents are different kinds of data.','Storage health, capacity, and tested recovery are essential to control-plane reliability.'],cue:'Remember the desired shop; separately protect the books.',plain:'Maya’s central ledger records which counters should exist. The books inside a storage locker are separate contents; copying the ledger does not copy those books.',story:'We inspect one ConfigMap through the API and distinguish its stored configuration from catalog data on a hypothetical PVC. The exercise never accesses the live datastore directly.',
detail:'Ordinary Kubernetes components use the API server rather than bypassing it to change etcd keys. API reads can involve caches, so a kubectl read is not proof of a direct disk read. Distribution-specific lightweight backends can differ from the standard etcd arrangement.\n\nThree members tolerate one member failure; five tolerate two while quorum remains. More members also add coordination work. Latency, leader changes, database quota, compaction, and disk health need monitoring. Backup frequency and recovery objectives belong to an explicit service requirement, not a universal hourly rule.',source:'[Operating etcd for Kubernetes](https://kubernetes.io/docs/tasks/administer-cluster/configure-upgrade-etcd/), [etcd disaster recovery](https://etcd.io/docs/v3.6/op-guide/recovery/)',
guide:'Read the [page ConfigMap](../../manifests/10-page.yaml) and [Deployment](../../manifests/20-deployment.yaml). They declare API state that the control plane persists. Live §metadata.uid§ identifies an object incarnation and §resourceVersion§ supports API concurrency/watch semantics; neither is an application backup.\n\nThe base stores only disposable configuration and serves it from a projected volume. A real database’s PVC data would live in its storage system, outside an ordinary etcd snapshot. There is no etcd Pod or snapshot command in this chapter because that would require control-plane credentials and an environment-specific recovery design.',
runbook:'## Prerequisites\n\nRead permission for the chapter objects. Platform/datastore details require administrator-provided evidence.\n\n~~~powershell\nkubectl -n k8s-learning-platform get configmap platform-catalog-page -o yaml\nkubectl -n k8s-learning-platform get deployment platform-catalog -o yaml\nkubectl get --raw=/readyz --request-timeout=5s\n~~~\n\nExpect stored configuration and an API readiness result where authorized. API readiness is not a full datastore health or backup test.\n\n## Recovery inventory\n\nList separately: authored manifests, live API objects, volume contents, external database records, certificates/keys, and recovery credentials. For each, name the owner and backup/restore method. Ask which datastore the actual distribution uses and where its restore procedure is tested.\n\n## Troubleshooting and cleanup\n\nAn API failure can have several causes; do not diagnose quorum loss from one timeout. No datastore keys or cluster configuration are changed, so there is no cleanup.'});
topic(57,{
summary:['API authentication identifies a caller and authorization checks its allowed action.','Applicable mutating admission can change a write request before final validation.','Validating admission accepts or rejects the proposed object.','Server dry-run evaluates supported write processing without persisting the object.'],cue:'Who, allowed action, proposed shape, admission, persistence.',plain:'Maya checks a worker’s identity, verifies their permission to submit a plan, fills required details, and reviews the final plan before filing it.',story:'A disposable Pod specification is sent through server dry-run. We read the returned defaults and then verify that no Pod was created.',
detail:'Not every API request is an object creation; reads do not follow the same mutating admission path as writes. Defaulting, schema checks, and admission have implementation details beyond a single simple diagram. Use the model to explain responsibilities, not to claim every request has identical stages.\n\nWebhook failure policy and timeout trade availability against enforcement. Fail-open is not universally appropriate for security controls. Dry-run requires participating webhooks to declare compatible side-effect behaviour; an unsupported webhook can cause the request to fail.',source:'[Admission controllers](https://kubernetes.io/docs/reference/access-authn-authz/admission-controllers/), [API dry-run](https://kubernetes.io/docs/reference/using-api/api-concepts/#dry-run)',
guide:'File: [admission preview Pod](manifests/10-admission-preview.yaml). It uses the same harmless worker/security settings as other exercises.\n\nThe runbook applies it with §--dry-run=server -o yaml§. The returned object can contain defaulted fields, assigned admission values, or webhook mutations; those are observations to compare with the authored input. §metadata.name: admission-preview§ gives the later nonexistence check an exact target.\n\nThis topic installs no webhook or admission policy. A successful preview says the request passed that server’s applicable checks at that time; it does not prove the image will run or that the caller can mutate every object.',
runbook:'## Prerequisites\n\nShared namespace, reachable API, and permission to create Pods. The name must be unused.\n\n~~~powershell\nkubectl -n k8s-learning-platform get pod admission-preview\nkubectl apply --dry-run=server -f topics/57-api-admission/manifests/10-admission-preview.yaml -o yaml\nkubectl -n k8s-learning-platform get pod admission-preview\n~~~\n\nExpect NotFound before and after, with a successfully returned Pod specification in between. If the name already exists, stop and inspect ownership before interpreting the result.\n\n## Explain the evidence\n\nIdentify authored versus server-returned fields, any warnings, and the distinction between Forbidden authorization errors and admission/schema rejection. A dry-run can still leave audit records even though it stores no Pod.\n\n## Troubleshooting and cleanup\n\nRead the exact webhook or validation error; do not disable admission to force a preview. No Pod should exist from this procedure, so no resource cleanup is needed.'});
topic(58,{
summary:['A CRD adds a custom resource type and its API schema.','A custom resource is one instance stored through that API.','A controller reconciles desired state; the CRD alone does not implement behaviour.','An Operator combines domain knowledge with reconciliation for application lifecycle tasks.'],cue:'Type, instance, controller, domain knowledge.',plain:'Maya invents a new order form. Filing a form does not pack an order; someone must read it and perform the appropriate work.',story:'The Certificate used in question 50 is a custom resource. Its CRD defines the form, while cert-manager’s controllers issue and maintain the certificate.',
detail:'Custom API design needs schema validation, status, versioning/conversion, authorization, and lifecycle decisions. Operators also need retry-safe reconciliation, ownership, finalizer handling, and upgrade/recovery procedures. Installing a CRD without its controller can leave accepted objects that never progress.\n\nAn Operator’s advertised maturity is not proof of safe database upgrades or backups. Evaluate the specific release, supported operations, failure modes, and restore evidence. Building one is an engineering commitment whose duration depends on scope.',source:'[Custom resources](https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/custom-resources/), [Operator pattern](https://kubernetes.io/docs/concepts/extend-kubernetes/operator/)',
guide:'Optional concrete references are the [Certificate](../50-certificates-and-tls/optional/20-certificate.yaml) and its [walkthrough](../50-certificates-and-tls/manifest-guide.md).\n\n§apiVersion: cert-manager.io/v1§ and §kind: Certificate§ name a custom API. Its §spec§ states the request. Controller-maintained §status.conditions§ describes progress, while the target Secret is a separate resource. The CRD’s scope is Namespaced, but the CRD definition itself is cluster-scoped.\n\nNo sample fake Operator is created. Reading an existing CRD and comparing an instance’s spec/status demonstrates the separation without claiming that schema installation automates a business process.',
runbook:'## Prerequisites\n\nDiscovery access. Optional inspection requires an existing cert-manager installation and permission to read its CRD.\n\n~~~powershell\nkubectl api-resources --api-group=cert-manager.io\nkubectl get crd certificates.cert-manager.io -o custom-columns=NAME:.metadata.name,SCOPE:.spec.scope,VERSIONS:.spec.versions[*].name\n~~~\n\nIf installed, expect the custom type, Namespaced scope, and served schema versions. If absent, use the authored Certificate example and mark live custom-API inspection skipped.\n\nIf you completed question 50 and kept its disposable objects, inspect §kubectl -n k8s-learning-platform get certificate catalog-lab -o yaml§. Identify desired fields, controller conditions, and the generated Secret. Use that question’s runbook if you want to repeat issuance.\n\n## Troubleshooting and cleanup\n\nAccepted-but-unready instances need controller/condition checks, not another CRD installation. Remove any question-50 resources using its cleanup. Never delete a shared CRD to clean up one instance.'});
topic(59,{
summary:['The scheduler finds unscheduled Pods and evaluates eligible nodes.','Filtering removes nodes that cannot satisfy required constraints.','Scoring ranks feasible nodes before subsequent placement stages.','schedulerName selects a scheduler; naming one does not install it.'],cue:'Can it fit, where is it preferred, who will place it?',plain:'Maya first removes rooms that cannot hold the counter, then compares the rooms that remain. Addressing the request to a nonexistent coordinator leaves it waiting.',story:'A tiny Pod references an intentionally absent scheduler name. It stays unassigned without consuming large resources or changing node labels.',
detail:'Requests, taints, affinity, volumes, and topology can affect feasibility. Scores depend on configured plugins and weights; “least busy node wins” is an unreliable shortcut. The scheduling framework includes stages beyond filtering/scoring, such as reserve, permit, and binding.\n\nMultiple schedulers must be configured and operated deliberately. A custom name is not a feature flag for smarter scheduling. Pending can also include post-scheduling startup, so inspect spec.nodeName, conditions, and container state to locate the wait.',source:'[Scheduling framework](https://kubernetes.io/docs/concepts/scheduling-eviction/scheduling-framework/), [multiple schedulers](https://kubernetes.io/docs/tasks/extend-kubernetes/configure-multiple-schedulers/)',
guide:'File: [custom-scheduler wait](manifests/10-custom-scheduler.yaml).\n\n§schedulerName: bookshop-uninstalled-scheduler§ directs scheduling to a controller that this lab does not install. The Linux selector and small resource request remain ordinary. No node is deliberately filled and no Pod is made preempting.\n\nThe expected live evidence is an empty §spec.nodeName§ while the Pod waits. The usual scheduler should not bind it on behalf of the named scheduler. The topic does not create a scheduler Deployment or mutate the shared catalog’s scheduling policy.',
runbook:'## Prerequisites\n\nShared namespace and confirmation that the practice scheduler name is unused.\n\n~~~powershell\nkubectl apply -f topics/59-scheduler-internals/manifests/\nkubectl -n k8s-learning-platform get pod scheduler-waiting -o wide\nkubectl -n k8s-learning-platform get pod scheduler-waiting -o jsonpath=\'{.spec.schedulerName}\'\nkubectl -n k8s-learning-platform describe pod scheduler-waiting\n~~~\n\nExpect Pending and no assigned node. Events may be sparse because the named scheduler is absent; do not require an Insufficient CPU message. If it gets assigned, the assumption about scheduler availability was wrong—inspect the environment.\n\n## Recovery and cleanup\n\nThe missing controller is the deliberate cause. Remove the disposable input rather than installing a cluster component.\n\n~~~powershell\nkubectl delete -f topics/59-scheduler-internals/manifests/ --ignore-not-found\n~~~\n\nExplain how this differs from a default-scheduler Pod rejected by filtering.'});
topic(60,{
summary:['CRI defines communication between kubelet and a container runtime service.','containerd and CRI-O are common CRI-capable runtime choices.','Removing built-in dockershim did not invalidate ordinary Docker-built images.','Runtime identity, image identity, and Kubernetes API version describe different layers.'],cue:'Build the image; ask through CRI; run the process.',plain:'Maya’s supplier packages a book, the ordering interface describes how to request delivery, and a courier carries it. Changing the courier does not automatically change the package format.',story:'We inspect the actual node’s reported runtime and the catalog container’s image ID. A Docker-labelled runtime is explained through CRI integration rather than declared impossible.',
detail:'Kubernetes removed its built-in dockershim integration in 1.24. Docker Engine can still be integrated through a maintained CRI adapter such as cri-dockerd. Compatibility depends on the runtime, adapter, Kubernetes version, and configuration.\n\nOCI image compatibility does not guarantee an image supports the node architecture, security settings, or every runtime feature. Node-level tools such as crictl need appropriate runtime endpoint access and are outside an ordinary application namespace workflow.',source:'[Container runtimes](https://kubernetes.io/docs/setup/production-environment/container-runtimes/), [dockershim removal FAQ](https://kubernetes.io/blog/2022/02/17/dockershim-faq/)',
guide:'Read the [base Deployment](../../manifests/20-deployment.yaml). §image: busybox:1.36§ names content to resolve; it does not select containerd, CRI-O, or Docker Engine. The node’s configured runtime starts the Pod sandbox and containers through the platform’s CRI path.\n\nLive node §status.nodeInfo.containerRuntimeVersion§ reports runtime identity/version. Container §imageID§ records resolved content, while §containerID§ identifies a running runtime object. None of those is the Pod UID. This chapter installs no runtime and changes no node service.',
runbook:'## Prerequisites\n\nBase Pod read permission; node information requires cluster-scoped read permission.\n\n~~~powershell\nkubectl get nodes -o custom-columns=NAME:.metadata.name,KUBELET:.status.nodeInfo.kubeletVersion,RUNTIME:.status.nodeInfo.containerRuntimeVersion\nkubectl -n k8s-learning-platform get pods -l app=platform-catalog -o yaml\n~~~\n\nRecord the actual runtime rather than predicting it. On the validation machine the node can report a Docker runtime through a CRI integration; that is compatible with dockershim having been removed from Kubernetes.\n\n## Explain the layers\n\nIdentify the image tag, resolved image ID, container ID, Pod UID, node name, and kubelet version. Explain why changing an image tag does not replace the node runtime.\n\n## Troubleshooting and cleanup\n\nAn image startup problem may involve architecture, pull access, permissions, or runtime configuration. Request node-side evidence from the platform owner when needed. No runtime or workload configuration is changed, so nothing needs cleanup.'});
