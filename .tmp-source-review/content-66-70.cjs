const {topic}=require('./new40-content.cjs');
topic(66,{
summary:['Requests reserve schedulable capacity while usage measures consumed resources.','Cost allocation links infrastructure spend to workloads, teams, and shared services.','Right-sizing needs historical demand and service performance evidence.','Reducing a request does not automatically reduce the cloud bill.'],cue:'Attribute, measure, resize, verify the bill and the service.',plain:'Maya measures the desks actually used, checks peak trading hours, and counts shared rent. Shrinking a desk does not save rent if the same room remains leased.',story:'We calculate the catalog’s declared CPU/memory across two replicas, inspect optional current usage, and draft a sizing decision with a clear evidence gap.',
detail:'Short idle samples can hide bursts, startup needs, and tail latency. Include safety margins, throttling/OOM evidence, and scheduling effects. Savings depend on whether infrastructure can actually be released and on the provider’s billing model.\n\nSpot/preemptible capacity needs interruption-tolerant application design; disruption budgets cannot prevent a provider from reclaiming capacity. Track persistent storage, load balancers, egress, observability, and idle/shared capacity too. Use current regional provider pricing instead of fixed savings percentages.',source:'[OpenCost allocation model](https://opencost.io/docs/specification/), [Kubernetes resource management](https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/)',
guide:'Read the [Deployment](../../manifests/20-deployment.yaml). Two web replicas each request §25m§ CPU and §32Mi§ memory, so steady requests total §50m§ and §64Mi§. Their limits total §200m§ and §128Mi§. Rollout surge and runtime/system overhead can add temporary or shared demand.\n\n§team: bookshop§ supports allocation only if the cost system ingests and uses it. The manifest does not express an hourly price. No request change or VPA is applied by this question; historical evidence must justify a real sizing change.',
runbook:'## Prerequisites\n\nBase inspection works without metrics. Usage checks require the resource metrics API; cost attribution needs an existing cost system.\n\n~~~powershell\nkubectl -n k8s-learning-reliability get deployment reliable-catalog -o yaml\nkubectl -n k8s-learning-reliability top pods -l app=reliable-catalog\n~~~\n\nCalculate the declared totals and compare with available usage. Missing metrics means the sizing conclusion remains incomplete.\n\n## Decision worksheet\n\nRecord historical peak/percentile usage, latency/error targets, throttling/OOM evidence, ownership labels, and the mechanism that would release billable capacity. Propose one change with a rollback trigger; do not make it from a single idle sample.\n\nIf no billing data is available, report resource quantities rather than inventing currency savings. No resources or billing settings change, so no cleanup is needed.'});
topic(67,{
summary:['Gateway API separates infrastructure, listener, and route responsibilities.','GatewayClass identifies an implementation, while Gateway defines listeners.','HTTPRoute attaches routing rules to a permitted parent and backend.','CRDs, a compatible controller, accepted attachment, and reachable listeners are all required.'],cue:'Class chooses the operator; Gateway opens the door; Route directs visitors.',plain:'The building team chooses reception equipment, operations opens a doorway, and Maya publishes which corridor leads to the catalog.',story:'An optional HTTPRoute sends a practice host to the catalog through an administrator-provided Gateway. Creating the route object alone is not treated as a working entry point.',
detail:'Ingress remains a supported API with a narrower standardized model; Gateway API offers richer role separation and routing capabilities whose support must be checked per implementation. Cross-namespace references can require explicit permissions such as ReferenceGrant; do not assume every reference is accepted.\n\nInspect per-parent route status, reference resolution, listener compatibility, and Gateway programming. A successful port-forward to the backend bypasses the Gateway and cannot prove this routing path.',source:'[Gateway API HTTP routing](https://gateway-api.sigs.k8s.io/guides/user-guides/http-routing/), [Gateway API overview](https://gateway-api.sigs.k8s.io/concepts/api-overview/)',
guide:'File: [HTTPRoute](optional/10-route.yaml). §gateway.networking.k8s.io/v1§ requires a served Gateway API. §parentRefs§ names §bookshop-gateway§ in the reliability namespace. The existing Gateway must allow this attachment and have a compatible HTTP listener.\n\n§hostnames: [bookshop.example.com]§ selects the request host. §PathPrefix /§ matches paths, and §backendRefs§ directs them to §reliable-catalog§ Service port 80, not the Pod port 8080. No GatewayClass, load balancer, public DNS record, or Gateway is created here. If the provided parent name differs, update that reference before application.',
runbook:'## Prerequisites\n\nExisting Gateway API/controller, ready base, and an approved compatible Gateway in this namespace. The route must fit its listener/allowedRoutes policy. If no parent is provided, render/read and optionally server-dry-run only.\n\n~~~powershell\nkubectl api-resources --api-group=gateway.networking.k8s.io\nkubectl -n k8s-learning-reliability get gateway bookshop-gateway -o yaml\nkubectl apply --dry-run=server -f topics/67-gateway-api/optional/\n~~~\n\nOnly with the intended parent available:\n\n~~~powershell\nkubectl apply -f topics/67-gateway-api/optional/\nkubectl -n k8s-learning-reliability get httproute bookshop-route -o yaml\ncurl.exe --fail --max-time 10 -H \'Host: bookshop.example.com\' http://PASTE_GATEWAY_ADDRESS\n~~~\n\nReplace the address with the parent’s reachable listener address. Expect Accepted/ResolvedRefs for the intended parent and the catalog response through that listener. Missing-parent dry-run success proves schema acceptance only.\n\n## Troubleshooting and cleanup\n\nCheck listener host/protocol, parent conditions, backend references, and actual address routing. Remove only this route if created:\n\n~~~powershell\nkubectl delete -f topics/67-gateway-api/optional/ --ignore-not-found\n~~~'});
topic(68,{
summary:['Large clusters increase API, datastore, scheduler, network, and add-on demands.','Object counts and change rates matter as well as node count.','Client watches, pagination, and scoped queries influence control-plane load.','Capacity limits are version- and provider-specific, not universal application guarantees.'],cue:'Measure the management workload, not only the machines.',plain:'Maya’s directory office can become busy even when many counters are idle. Frequent address changes and repeated full-directory requests also consume staff time.',story:'We inspect a small namespace and reason about how its Pods, events, metrics, and endpoint changes would grow across many branches. No stress test is launched.',
detail:'Published Kubernetes scale envelopes assume multiple simultaneous limits and defined test conditions. A provider or workload can hit another bottleneck earlier. Add-ons such as DNS, metrics, logging, admission, and network controllers need their own capacity planning.\n\nPrefer efficient list/watch patterns and avoid uncontrolled polling. API Priority and Fairness manages competing API requests; scheduler priority solves a different problem. Do not change cache sizes, scheduler parallelism, or etcd maintenance settings without measured evidence and an operating plan.',source:'[Large cluster considerations](https://kubernetes.io/docs/setup/best-practices/cluster-large/), [API Priority and Fairness](https://kubernetes.io/docs/concepts/cluster-administration/flow-control/)',
guide:'Read the [Deployment](../../manifests/20-deployment.yaml) and generated EndpointSlices from [question 55](../../../05-bookshop-platform/topics/55-endpointslices/manifest-guide.md). Each desired replica adds Pod/controller/status activity, not just CPU usage. Rolling updates and failing workloads add churn.\n\nNo scale-test manifest is included. The base’s two replicas provide a unit for estimating object counts and event rates, but multiplying those quantities cannot predict end-to-end cluster capacity. Separate API traffic, controller reconciliation, scheduler work, and data-plane traffic in the model.',
runbook:'## Prerequisites\n\nRead access to the lab namespace; broader platform metrics are optional.\n\n~~~powershell\nkubectl -n k8s-learning-reliability get deploy,rs,pods,svc\nkubectl -n k8s-learning-reliability get endpointslices -l kubernetes.io/service-name=reliable-catalog\nkubectl -n k8s-learning-reliability get events --sort-by=.metadata.creationTimestamp\n~~~\n\n## Capacity model\n\nList steady object counts and the extra churn from a rollout, crash loop, or scaling burst. For each likely bottleneck, name a measurable signal and its owner. Include IP quotas, storage attachments, admission latency, and monitoring cardinality.\n\nIf a large-cluster dashboard is provided, compare actual signals with the current supported envelope for that platform. A successful two-Pod exercise is not a scalability benchmark. No load, tuning, or infrastructure change occurs, so cleanup is unnecessary.'});
topic(69,{
summary:['Admission policy can enforce selected rules on incoming workload changes.','Kyverno and Gatekeeper provide different policy languages and controller models.','Image digests, signatures, provenance, and vulnerability findings answer different questions.','Policy effectiveness needs positive, negative, and exemption-aware checks.'],cue:'Identify content, verify trust, enforce a clear rule.',plain:'Maya checks the exact edition of a supplier manual, its signature, and its inspection history. Knowing the edition alone does not prove the supplier is trusted.',story:'We review the catalog’s labels, resources, image reference, and security fields against a proposed policy. Existing policy engines are inspected only when available.',
detail:'Gatekeeper integrates OPA/Rego and constraint templates; Kyverno offers Kubernetes-oriented policy resources with capabilities dependent on version and rule type. Enforcement, audit/background reporting, exceptions, and existing-object coverage differ. Check the installed policy rather than assuming that a healthy controller blocks everything intended.\n\nA digest pins content but does not authenticate its publisher. Signature/provenance checks also need a trust policy. Vulnerability results change with scanner databases and do not prove exploitability or safety by themselves. Policy rollout needs observable failure handling and a review process.',source:'[Kyverno documentation](https://kyverno.io/docs/), [Gatekeeper documentation](https://open-policy-agent.github.io/gatekeeper/website/docs/)',
guide:'Read the [Deployment](../../manifests/20-deployment.yaml). It includes requests/limits, non-root controls, and ownership labels, but uses a mutable image tag. That is a visible gap if the proposed policy requires digests.\n\nNo policy resource is installed. A namespaced workload rule and cluster-wide webhook/constraint infrastructure have different blast radii. Match the actual policy API/schema and scope before creating rules; a generic untested ClusterPolicy would not be a meaningful demonstration here. Existing Secret/certificate contents should not be printed while reviewing policy.',
runbook:'## Prerequisites\n\nBase read access. Live policy observations require an existing engine, named test policy, and permission to inspect its decisions.\n\n~~~powershell\nkubectl api-resources --api-group=kyverno.io\nkubectl api-resources --api-group=templates.gatekeeper.sh\nkubectl -n k8s-learning-reliability get deployment reliable-catalog -o yaml\n~~~\n\n## Policy review\n\nDefine one rule, such as required team ownership or a trusted image identity. State scope, allow/deny examples, exception owner, and the evidence needed to show enforcement. Compare the authored workload with that rule.\n\nWith an approved existing test policy, use its supplied server-dry-run inputs and inspect the exact rejection reason. Without one, record enforcement untested rather than calling the manifest compliant with an unspecified standard.\n\nNo engine or policy is installed; no cleanup is needed.'});
topic(70,{
summary:['Node autoscalers provision or remove capacity through platform integrations.','Cluster Autoscaler commonly works with configured node groups.','Karpenter uses provider integrations and workload/node-pool constraints to provision capacity.','Scaling and consolidation depend on feasibility, limits, availability, and disruption rules.'],cue:'Work needs a room; the provider must actually supply one.',plain:'Maya can expand a predefined set of rooms or ask a provider for rooms matching current needs. Both approaches still depend on budget, building availability, and moving people safely.',story:'We review node capacity and the catalog’s scheduling constraints without creating an unschedulable load or requesting cloud machines.',
detail:'Compare supported providers, node lifecycle, instance constraints, operational ownership, and workload diversity for the actual implementation. Avoid universal “seconds versus minutes” or “always cheaper” rankings. Neither controller can repair a misspelled affinity label or an impossible storage topology by adding arbitrary nodes.\n\nConsolidation and interruption handling need capacity and workload cooperation. Provider quotas, maximum sizes, budgets, and unavailable instance offerings can block provisioning. Node autoscaling is separate from HPA’s replica decisions and VPA’s resource sizing.',source:'[Kubernetes node autoscaling](https://kubernetes.io/docs/concepts/cluster-administration/node-autoscaling/), [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler), [Karpenter NodePools](https://karpenter.sh/docs/concepts/nodepools/)',
guide:'The [Deployment](../../manifests/20-deployment.yaml) requests small resources and Linux placement. These constraints become part of capacity feasibility; observed low CPU alone does not mean a Pod can schedule.\n\nNo NodePool, NodeClass, cloud credential, or node group is authored. Those definitions depend on the provider and could create billable infrastructure. An existing controller may be managed outside visible cluster Pods, so discovery output alone cannot identify the full autoscaling arrangement.',
runbook:'## Prerequisites\n\nNode read permission and platform documentation. No node autoscaler is required for this design exercise.\n\n~~~powershell\nkubectl get nodes -o wide\nkubectl -n k8s-learning-reliability get pods -o wide\nkubectl -n k8s-learning-reliability get deployment reliable-catalog -o yaml\nkubectl api-resources --api-group=karpenter.sh\n~~~\n\n## Comparison exercise\n\nRecord the actual provider, existing node-group/pool model, limits, eligible instance types, disruption settings, and infrastructure owner. Explain how a CPU shortage, wrong node label, and unavailable storage zone would lead to different responses.\n\nIf no controller is installed, compare designs using those requirements and mark runtime provisioning untested. Do not deliberately fill the cluster to provoke scaling. No infrastructure changes occur, so no cleanup is needed.'});
