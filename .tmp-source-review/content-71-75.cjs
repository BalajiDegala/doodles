const {topic}=require('./new40-content.cjs');
topic(71,{
summary:['EKS, GKE, and AKS provide managed Kubernetes services with different operating models.','Managed control planes still leave workload and data responsibilities to the customer.','Compute modes, identity, networking, upgrades, and billing require separate comparison.','Choose using current regional capabilities and team requirements, not a universal ranking.'],cue:'Compare responsibilities before comparing logos.',plain:'Maya compares managed buildings: each supplies some maintenance, but rent, access rules, room options, and responsibilities differ.',story:'The same small catalog is used as a requirements checklist for AWS, Google Cloud, and Azure. No cloud account or cluster is created.',
detail:'EKS integrates with AWS IAM, compute, and networking options. GKE offers Standard and Autopilot operating models. AKS integrates with Azure infrastructure and identity services. The exact responsibility split depends on the selected mode and features.\n\nCompare control-plane/compute charges, regional availability, supported versions, storage, egress, observability, identity, and upgrade policies using dated provider evidence. Avoid static claims that a control plane is always free, a provider is always most secure, or one service automatically operates the whole application.',source:'[Amazon EKS overview](https://docs.aws.amazon.com/eks/latest/userguide/what-is-eks.html), [GKE overview](https://cloud.google.com/kubernetes-engine/docs/concepts/kubernetes-engine-overview), [AKS core concepts](https://learn.microsoft.com/en-us/azure/aks/core-aks-concepts)',
guide:'The [Deployment](../../manifests/20-deployment.yaml) and [Service](../../manifests/30-service.yaml) use portable API shapes, but portability also depends on supported versions, Linux image execution, admission rules, and network access.\n\nThe base has no cloud load balancer, provider IAM mapping, storage class, or public ingress configuration. Those omissions make it a useful initial compatibility probe but not a complete cloud architecture. A real deployment must add platform-specific identity, storage, routing, and recovery decisions.',
runbook:'## Prerequisites\n\nThe local files and provider documentation are enough. Do not provision resources to complete this comparison.\n\n~~~powershell\nkubectl -n k8s-learning-reliability get deployment reliable-catalog -o yaml\nkubectl -n k8s-learning-reliability get service reliable-catalog -o yaml\n~~~\n\n## Comparison worksheet\n\nFor each provider, record the candidate region and compute mode, customer/provider responsibilities, identity approach, network/IP model, storage options, upgrade process, availability needs, and dated cost inputs. Include taxes/support/egress assumptions where relevant to an actual estimate.\n\nGive a conditional recommendation for Maya’s stated requirements and list missing evidence. A local manifest validation is not proof of deployment on all three services. No account, billing setting, or infrastructure changes occur; no cleanup is needed.'});
topic(72,{
summary:['API group and version select the schema a client requests.','Deprecation signals migration work before a future compatibility problem.','Removal and support rules differ by API stability level and resource.','Migration requires checking field semantics and controllers as well as changing apiVersion.'],cue:'Inventory the forms before the old counter closes.',plain:'Maya’s office announces a replacement order form. Renaming the form’s heading does not fix changed fields or a workflow that still expects the old format.',story:'We inventory the base APIs and optional custom APIs, then identify what evidence is needed before the upgrade planned in question 64.',
detail:'There is no blanket “two releases of notice” rule for all Kubernetes APIs. Consult the deprecation policy and migration guide for the exact type/version. Served API discovery proves current availability, not compatibility with a proposed future cluster.\n\nTools can flag known deprecated APIs, but coverage may differ for files, rendered charts, live objects, and custom resources. A resource can be deprecated while its apiVersion string remains v1, as with legacy Endpoints. Test the rendered configuration and controller behaviour against the target environment.',source:'[API deprecation policy](https://kubernetes.io/docs/reference/using-api/deprecation-policy/), [deprecated API migration guide](https://kubernetes.io/docs/reference/using-api/deprecation-guide/)',
guide:'The base uses §apps/v1§ Deployment and core §v1§ Namespace, ConfigMap, and Service. Optional files introduce controller-defined APIs such as §gateway.networking.k8s.io/v1§ and §keda.sh/v1alpha1§. A custom API’s version label does not follow the same lifecycle as every core API.\n\nInspect the [Deployment](../../manifests/20-deployment.yaml), [HTTPRoute](../67-gateway-api/optional/10-route.yaml), and [ScaledObject](../74-coordinated-autoscaling/optional/20-scaledobject.yaml). Schema availability and controller availability are separate dependencies. No deliberately removed API is applied to create a failure.',
runbook:'## Prerequisites\n\nLocal file access and API discovery permission. Target-version checks need a suitable test environment or approved schema/tooling.\n\n~~~powershell\nGet-ChildItem -LiteralPath manifests -Filter *.yaml | Select-String -Pattern \'^apiVersion:\'\nkubectl api-versions\nkubectl apply --dry-run=server -f manifests/\n~~~\n\nExpect the base API versions and a successful current-server preview once the namespace exists. This does not validate a future upgrade target.\n\n## Migration record\n\nFor each flagged API, record where it appears, the replacement, field/behaviour changes, controller compatibility, target-version proof, and recovery plan. Include rendered templates and external controllers, not just top-level files.\n\n## Cleanup\n\nNo resources are created by these checks. An unknown kind can mean a missing CRD rather than a removed core API; diagnose the exact group/version.'});
topic(73,{
summary:['Native sidecars are restartable init containers with restartPolicy: Always.','Their startup sequencing can let a helper become available before main work.','Native sidecars do not keep a completed Job running indefinitely.','They have different lifecycle semantics from two ordinary application containers.'],cue:'Start the helper first; let the report finish.',plain:'Maya opens a helper desk before a report worker starts. Once the report is complete, the helper should close instead of keeping the whole task marked unfinished.',story:'A helper creates a ready marker and emits heartbeats. A report checks that marker and exits successfully; the Job can complete while Kubernetes shuts the sidecar down.',
detail:'Native sidecars are stable from Kubernetes 1.33. A startup probe can determine when the restartable init container is considered started for sequencing. Main-container completion drives the Job outcome rather than waiting forever for the helper’s loop.\n\nA helper running under ordinary containers lacks these native ordering/completion rules. Resource requests, probe health, and shutdown budgets still matter. The file marker is a teaching signal, not a full dependency-health protocol.',source:'[Native sidecar containers](https://kubernetes.io/docs/concepts/workloads/pods/sidecar-containers/)',
guide:'File: [native sidecar Job](manifests/10-sidecar-job.yaml).\n\nThe Pod has §restartPolicy: Never§ for its report. The §helper§ entry is under §initContainers§ and overrides its own restart policy to Always. It writes §/signals/ready§ to a shared emptyDir. A one-second startup probe checks that file with up to 30 failures before treating startup as failed.\n\nThe §report§ container uses §set -eu§, verifies the marker, prints completion, and exits. It mounts the same volume read-only. §backoffLimit: 0§ keeps failed evidence simple. Both containers declare resources and the shared non-root controls. A container-wide sleep loop here is not meant to do application work.',
runbook:'## Prerequisites\n\nKubernetes 1.33+ for stable native sidecars, compatible kubectl, and the shared namespace. Verify cluster support before applying.\n\n~~~powershell\nkubectl version -o yaml\nkubectl apply --dry-run=server -f topics/73-native-sidecars/manifests/\nkubectl apply -f topics/73-native-sidecars/manifests/\nkubectl -n k8s-learning-reliability wait --for=condition=Complete job/native-report --timeout=120s\nkubectl -n k8s-learning-reliability logs job/native-report -c report\nkubectl -n k8s-learning-reliability logs job/native-report -c helper\nkubectl -n k8s-learning-reliability get pods -l exercise=native-sidecar -o yaml\n~~~\n\nExpect Bookshop report finished and a completed Job even though the helper’s command is an endless loop. Inspect initContainerStatuses and containerStatuses separately; helper shutdown status need not match the report’s successful exit.\n\n## Troubleshooting and cleanup\n\nCheck startup probe, marker permissions, feature support, and admission if completion fails. Do not move the helper under ordinary containers and claim equivalent behaviour.\n\n~~~powershell\nkubectl delete -f topics/73-native-sidecars/manifests/ --ignore-not-found\n~~~'});
topic(74,{
summary:['HPA changes replica count using observed metrics.','VPA recommends or adjusts resource sizing through its installed controller.','KEDA connects supported event sources to scaling, including activation from zero.','Controllers must coordinate ownership and the meaning of their metrics.'],cue:'Copies, size, events, then room for all of them.',plain:'Maya can open more counters, resize desks, and schedule opening hours. Two managers changing the same counter-count instruction independently can undo each other’s decisions.',story:'An optional KEDA cron trigger controls a separate event-counter Deployment. It needs no external queue credentials and demonstrates scheduled activation rather than queue processing.',
detail:'KEDA commonly manages an HPA for scaling above zero while handling activation through its controllers. Do not attach an independent HPA to the same target. CPU-utilization HPA and VPA request changes can interact through the utilization denominator.\n\nEvent scaling still needs available nodes, metrics/controller health, bounds, and an application that tolerates cold starts. Scaling to zero is unsuitable when required work cannot be represented by a usable activation signal. Node autoscaling remains a separate capacity layer.',source:'[KEDA ScaledObject](https://keda.sh/docs/2.20/reference/scaledobject-spec/), [KEDA cron scaler](https://keda.sh/docs/2.20/scalers/cron/)',
guide:'Files: [target Deployment](manifests/10-event-counter.yaml) and [optional ScaledObject](optional/20-scaledobject.yaml).\n\nThe target uses the base page but a separate app label and omits authored replicas so an autoscaler can own scale. §scaleTargetRef.name: event-counter§ links the ScaledObject to it. Bounds are 0-2 replicas, polling is 15 seconds, and cooldown is 30 seconds.\n\nThe cron trigger uses §Asia/Kolkata§, start §0 9 * * *§, end §0 17 * * *§, and string §desiredReplicas: "2"§. These are example business hours, not an immediate test window. Adjust start/end to a short future window before live practice. KEDA owns any generated HPA; this file does not author a second one.',
runbook:'## Prerequisites\n\nExisting healthy KEDA CRDs/controllers/metrics integration, the base ConfigMap, and spare capacity. If KEDA is absent, skip application. Choose a short future cron window in the declared timezone before applying the optional file.\n\n~~~powershell\nkubectl api-resources --api-group=keda.sh\nkubectl apply -f topics/74-coordinated-autoscaling/manifests/\nkubectl apply --dry-run=server -f topics/74-coordinated-autoscaling/optional/\nkubectl apply -f topics/74-coordinated-autoscaling/optional/\nkubectl -n k8s-learning-reliability get scaledobject bookshop-hours -o yaml\nkubectl -n k8s-learning-reliability get hpa\nkubectl -n k8s-learning-reliability get deployment event-counter -w\n~~~\n\nStop the watch with Ctrl+C after the chosen window. Expect activity toward two replicas during the window and eventual zero after it, allowing polling/cooldown/controller delays. Inspect Ready/Active conditions; exact second-by-second scaling is not promised.\n\n## Troubleshooting and cleanup\n\nCheck timezone/window, target name, controller conditions, and generated HPA ownership. Remove both resources even if the observation was skipped after setup:\n\n~~~powershell\nkubectl delete -f topics/74-coordinated-autoscaling/optional/ --ignore-not-found\nkubectl delete -f topics/74-coordinated-autoscaling/manifests/ --ignore-not-found\n~~~'});
topic(75,{
summary:['Zero-trust design avoids granting access merely because traffic is inside a network.','Identity, authentication, authorization, and encryption require explicit decisions.','NetworkPolicy can restrict selected traffic but is not cryptographic workload identity.','A useful verification checks allowed, denied, and recovery paths.'],cue:'Location is not permission.',plain:'Maya does not let every person inside the building enter the stockroom. A door rule, a verified staff badge, and a protected conversation each solve a different problem.',story:'Two visitors initially fetch the catalog. A scoped ingress rule permits only the approved label; deleting the rule restores access. The lesson then identifies the identity and encryption controls this test does not provide.',
detail:'Labels are mutable metadata subject to API permissions. A user able to create arbitrarily labeled Pods can potentially satisfy this rule, so label filtering alone is not strong identity verification. RBAC, workload identity, application authorization, and transport protection need their own design.\n\nNetworkPolicy allow rules add together and require an enforcing network implementation. This example restricts ingress to the catalog only and leaves DNS/client egress alone. It demonstrates segmentation on one path, not complete zero-trust architecture.',source:'[NetworkPolicy](https://kubernetes.io/docs/concepts/services-networking/network-policies/), [Kubernetes multi-tenancy](https://kubernetes.io/docs/concepts/security/multi-tenancy/)',
guide:'Files: [visitors](manifests/10-visitors.yaml) and [catalog policy](policy/20-catalog-ingress.yaml).\n\n§podSelector.app: reliable-catalog§ chooses the protected receivers. The same-namespace ingress peer selects §access: approved§. TCP 8080 is the destination Pod port, even though clients request the Service on port 80. §policyTypes: [Ingress]§ does not isolate client egress.\n\n§trusted-visitor§ has the approved label and §unknown-visitor§ does not. The names themselves confer no trust. No namespace-wide default deny, credentials, certificates, or mesh configuration is created.',
runbook:'## Prerequisites\n\nReady base and a NetworkPolicy-enforcing CNI. Inspect existing namespace policies first.\n\n~~~powershell\nkubectl -n k8s-learning-reliability get networkpolicy\nkubectl apply -f topics/75-zero-trust/manifests/\nkubectl -n k8s-learning-reliability wait --for=condition=Ready pod/trusted-visitor pod/unknown-visitor --timeout=120s\nkubectl -n k8s-learning-reliability exec trusted-visitor -- wget -T 3 -qO- http://reliable-catalog\nkubectl -n k8s-learning-reliability exec unknown-visitor -- wget -T 3 -qO- http://reliable-catalog\nkubectl apply -f topics/75-zero-trust/policy/\nkubectl -n k8s-learning-reliability exec trusted-visitor -- wget -T 3 -qO- http://reliable-catalog\nkubectl -n k8s-learning-reliability exec unknown-visitor -- wget -T 3 -qO- http://reliable-catalog\n~~~\n\nBoth must work before policy. After propagation, approved succeeds and unapproved fails with nonzero exit. If both still work, inspect CNI support and other additive allows.\n\n## Restore and clean up\n\n~~~powershell\nkubectl delete -f topics/75-zero-trust/policy/ --ignore-not-found\nkubectl -n k8s-learning-reliability exec unknown-visitor -- wget -T 3 -qO- http://reliable-catalog\nkubectl delete -f topics/75-zero-trust/manifests/ --ignore-not-found\n~~~\n\nAllow propagation and confirm restoration. Do not use port-forwarding as the policy test.'});
