const {topic}=require('./new40-content.cjs');
topic(46,{
summary:['A headless Service uses clusterIP: None instead of a virtual Service IP.','With selectors, its DNS answers expose backing endpoint addresses.','Clients are responsible for choosing and reconnecting to those endpoints.','Stable per-Pod names need the relevant hostname/subdomain or StatefulSet configuration.'],cue:'One name, individual destinations.',
plain:'Maya gives a caller the counter directory instead of connecting the call through reception. The caller now chooses a counter and handles a disconnected number.',story:'The same catalog Pods back a normal Service and a new headless Service. Comparing DNS answers reveals the difference without creating a database.',
detail:'Headless discovery can support peer membership and client-side balancing. DNS results can be cached and readiness affects published endpoints unless configuration requests otherwise. Returning several addresses does not guarantee fair traffic distribution.\n\nOur Deployment replicas have no stable ordinal identities. A headless Service alone does not turn them into StatefulSet replicas or create application replication. The earlier stateful storage lab shows that separate pattern.',source:'[Headless Services](https://kubernetes.io/docs/concepts/services-networking/service/#headless-services), [Service and Pod DNS](https://kubernetes.io/docs/concepts/services-networking/dns-pod-service/)',
guide:'File: [catalog-peers Service](manifests/10-headless.yaml).\n\n§clusterIP: None§ is the literal string requesting no virtual cluster IP. §selector.app: platform-catalog§ matches the existing Deployment. Port 80 still describes the Service port, with named target §http§ resolving to 8080.\n\nA client receiving Pod addresses connects to the actual Pod listener, port 8080; DNS A/AAAA records contain addresses, not port translation. Named SRV records can convey ports for clients that use them. The existing normal Service instead remains reachable on its virtual IP at port 80.',
runbook:'## Prerequisites\n\nBase replicas must be ready, with working cluster DNS.\n\n~~~powershell\nkubectl apply -f topics/46-headless-services/manifests/\nkubectl -n k8s-learning-platform get svc platform-catalog catalog-peers\nkubectl -n k8s-learning-platform get pods -l app=platform-catalog -o wide\nkubectl -n k8s-learning-platform exec deployment/platform-catalog -- nslookup platform-catalog\nkubectl -n k8s-learning-platform exec deployment/platform-catalog -- nslookup catalog-peers\nkubectl -n k8s-learning-platform exec deployment/platform-catalog -- wget -T 3 -qO- http://catalog-peers:8080\n~~~\n\nExpect the normal Service IP in the first answer and backing Pod addresses in the second. The HTTP request should return the page using a selected Pod address. DNS tools may print multiple sections or IPv4/IPv6 results.\n\n## Troubleshooting and cleanup\n\nIf no peers appear, inspect ready EndpointSlices and selectors; allow DNS propagation. One successful request is not balancing proof.\n\n~~~powershell\nkubectl -n k8s-learning-platform get endpointslices -l kubernetes.io/service-name=catalog-peers\nkubectl delete -f topics/46-headless-services/manifests/ --ignore-not-found\n~~~'});
topic(47,{
summary:['An ephemeral container adds debugging tools to an existing Pod.','It shares the Pod network but has its own image filesystem.','Target-process visibility depends on runtime support and permissions.','A completed ephemeral container is not individually removed or restarted.'],cue:'Borrow tools; preserve the evidence; replace the lab Pod to reset.',
plain:'An inspector brings a toolbox into Maya’s counter room. Sharing the room does not make the inspector’s toolbox identical to the worker’s cupboards.',story:'We create a disposable sleeping worker, attach a restricted inspector, and inspect its process/network view. The main catalog remains separate.',
detail:'Ephemeral containers are useful when an application image lacks a shell. They use a special Pod subresource and require appropriate authorization/admission. They are not ordinary extra Deployment containers and do not support the full ordinary-container configuration surface.\n\nThe target flag requests process-namespace targeting where supported; it is not a promise of filesystem access or elevated privileges. Exiting ends the inspector process but leaves its recorded specification/status on the Pod. Reset the disposable Pod for another clean exercise.',source:'[Debug running Pods](https://kubernetes.io/docs/tasks/debug/debug-application/debug-running-pod/), [ephemeral containers](https://kubernetes.io/docs/concepts/workloads/pods/ephemeral-containers/)',
guide:'File: [debug target](manifests/10-debug-target.yaml). It creates Pod §debug-counter§ with container §counter§, a non-root BusyBox process, and no API token.\n\nThe runbook’s §kubectl debug§ command creates ephemeral container §inspector§ rather than applying another Pod manifest. §--target=counter§ requests visibility into that target’s processes; §--profile=restricted§ requests restrictive debug settings. §--image=busybox:1.36§ chooses tools independently of the target image. The finite inspection command exits after printing process and resolver information.\n\nNo node-debug, privileged profile, or host filesystem mount is used.',
runbook:'## Prerequisites\n\nA kubectl supporting the restricted debug profile, an ephemeral-container-capable cluster/runtime, and permission to update §pods/ephemeralcontainers§. If admission blocks the profile, inspect the authored example and skip injection.\n\n~~~powershell\nkubectl apply -f topics/47-ephemeral-debugging/manifests/\nkubectl -n k8s-learning-platform wait --for=condition=Ready pod/debug-counter --timeout=120s\nkubectl -n k8s-learning-platform debug pod/debug-counter --container=inspector --image=busybox:1.36 --target=counter --profile=restricted --attach=false -- sh -c \'ps; cat /etc/resolv.conf\'\nkubectl -n k8s-learning-platform logs debug-counter -c inspector\nkubectl -n k8s-learning-platform get pod debug-counter -o yaml\n~~~\n\nRetry the log read after inspector startup if needed. Expect resolver data and process output, plus an ephemeral container entry. Missing target processes can mean unsupported targeting; it does not justify escalating the profile.\n\n## Cleanup\n\nThe inspector’s terminated record remains until the Pod is removed.\n\n~~~powershell\nkubectl delete -f topics/47-ephemeral-debugging/manifests/ --ignore-not-found\n~~~'});
topic(48,{
summary:['Topology spread constraints compare matching Pods across labeled failure domains.','maxSkew expresses the allowed or preferred imbalance for the selected mode.','DoNotSchedule enforces a hard constraint; ScheduleAnyway gives a preference.','Scheduling-time spreading does not continuously rebalance existing Pods.'],cue:'Count by domain, then place the next counter.',
plain:'Maya counts counters in each room before placing another. She can require balance or prefer it when room availability makes perfect balance impractical.',story:'Three display replicas prefer spreading across node hostnames. A one-node cluster can still run them, but cannot demonstrate cross-node resilience.',
detail:'The selector determines which peers count, and eligible domains depend on labels and scheduling constraints. A hostname domain is a node, not an availability zone. Hard constraints can leave Pods Pending when no eligible placement fits.\n\nPod anti-affinity can also use different topology keys; it is not inherently limited to nodes. Select the rule by the intended relationship and the available failure domains. Use readiness, capacity headroom, and disruption controls alongside placement rules.',source:'[Pod topology spread constraints](https://kubernetes.io/docs/concepts/scheduling-eviction/topology-spread-constraints/)',
guide:'File: [topology display Deployment](manifests/10-spread.yaml).\n\n§replicas: 3§ creates three displays. §topologyKey: kubernetes.io/hostname§ groups nodes by hostname. §labelSelector.matchLabels.app: topology-counters§ counts only this exercise’s replicas. §maxSkew: 1§ and §whenUnsatisfiable: ScheduleAnyway§ express a preference to reduce imbalance, not an availability guarantee.\n\nThe Deployment has its own selector but mounts the existing §platform-catalog-page§. It needs no new Service because this exercise observes placement. The general web/security fields are unchanged from the chapter base.',
runbook:'## Prerequisites\n\nBase ConfigMap and spare capacity for three small replicas. Multiple eligible Linux nodes are needed to observe cross-node spreading.\n\n~~~powershell\nkubectl get nodes -L kubernetes.io/hostname,topology.kubernetes.io/zone\nkubectl apply -f topics/48-topology-spread/manifests/\nkubectl -n k8s-learning-platform rollout status deployment/topology-counters --timeout=120s\nkubectl -n k8s-learning-platform get pods -l app=topology-counters -o wide\n~~~\n\nCount replicas by node. On one node all three share the same domain; record that limitation. On several nodes the preference competes with other scheduler scores and constraints.\n\n## Troubleshooting and cleanup\n\nRead Pod events for resource, taint, or node-label issues. Do not add fake zone labels or change node taints to produce a desired diagram.\n\n~~~powershell\nkubectl -n k8s-learning-platform describe pods -l app=topology-counters\nkubectl delete -f topics/48-topology-spread/manifests/ --ignore-not-found\n~~~'});
topic(49,{
summary:['PriorityClass assigns scheduling importance through a numeric priority.','Higher priority can influence queue order and preemption decisions.','preemptionPolicy: Never prevents that Pod from preempting lower-priority Pods.','Priority does not bypass placement rules or guarantee immediate scheduling.'],cue:'Urgency changes order; it does not create space.',
plain:'Maya can put an urgent order near the front of a queue. That does not create a free counter, and she may forbid removing another customer to make room.',story:'We inspect the catalog’s assigned priority and any existing classes, then design a non-preempting policy for reports. No pressure or real eviction is generated.',
detail:'PriorityClass is cluster-scoped. A global default affects Pods without an explicit class, so a casual teaching class can have effects outside the namespace. System-critical classes belong to system infrastructure rather than ordinary catalog workloads.\n\nNon-preempting Pods can still be preempted by other eligible higher-priority Pods. Preemption must leave a feasible placement, and handling of disruption budgets during preemption is best-effort rather than a universal block. Choose priorities with platform owners and test the workload’s own interruption behaviour.',source:'[Pod priority and preemption](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/)',
guide:'Read the [base Deployment](../../manifests/20-deployment.yaml). Its Pod template omits §priorityClassName§. Admission may supply the current global default; otherwise a normal default priority applies. Inspect the live Pod’s §spec.priority§ and §spec.preemptionPolicy§ instead of assuming the cluster’s policy.\n\nA design for a practice PriorityClass would explicitly choose a non-system name, a reviewed §value§, §globalDefault: false§, and §preemptionPolicy: Never§. No PriorityClass YAML is applied here because its scope is cluster-wide. Referencing a nonexistent class is an admission problem, not proof of scheduling priority.',
runbook:'## Prerequisites\n\nRead access to the base Pods; class inspection additionally needs cluster-scoped read permission.\n\n~~~powershell\nkubectl -n k8s-learning-platform get pods -l app=platform-catalog -o custom-columns=NAME:.metadata.name,CLASS:.spec.priorityClassName,PRIORITY:.spec.priority,PREEMPTION:.spec.preemptionPolicy\nkubectl get priorityclasses\n~~~\n\nExpect the live assigned values. An absent authored class and an observed default are compatible. If class access is denied, use the Pod fields and request the policy definition from the owner.\n\n## Design proof\n\nWrite a proposed class name, priority value, global-default choice, and preemption policy for a non-urgent catalog report. Explain which workloads could preempt it and why it may still wait for capacity. Have the platform owner assess the proposal before any future installation.\n\n## Cleanup\n\nNo class, node, or workload is changed; nothing needs removal.'});
topic(50,{
summary:['cert-manager reconciles Certificate resources using configured issuers.','An Issuer is namespaced; a ClusterIssuer can serve requests across namespaces.','A Certificate requests identities and stores resulting material in a Secret.','Successful issuance does not by itself establish client trust or configure a TLS listener.'],cue:'Request, issue, store, trust, serve.',
plain:'Maya can print a badge, store it safely, and present it at the door. Visitors still need a reason to trust the badge issuer, and the doorway must actually check it.',story:'An existing cert-manager installation issues a short-lived self-signed certificate for a practice catalog name. We inspect readiness, expiry, and Secret type without printing private keys.',
detail:'A self-signed issuer is useful for demonstrating issuance but does not create public browser trust. Production issuance also involves issuer authentication, DNS or HTTP validation where relevant, renewal, trust distribution, and application reload.\n\nThe certificate’s DNS names must match client expectations. Rotating Secret data does not guarantee an application has loaded the new certificate. This exercise never installs an ingress controller, requests a public certificate, or claims that the HTTP-only BusyBox website now serves TLS.',source:'[cert-manager Certificate resource](https://cert-manager.io/docs/usage/certificate/), [SelfSigned issuer](https://cert-manager.io/docs/configuration/selfsigned/)',
guide:'Files: [self-signed Issuer](optional/10-issuer.yaml) and [Certificate](optional/20-certificate.yaml).\n\nBoth use §cert-manager.io/v1§ and the platform namespace. §issuerRef§ names §bookshop-selfsigned§ as an Issuer. §secretName: catalog-lab-tls§ names the generated Secret. The DNS SAN is §platform-catalog.k8s-learning-platform.svc§; the subject includes a training organization.\n\n§duration: 24h§ and §renewBefore: 8h§ request a short practice lifetime and renewal window. ECDSA size 256 selects the key type; explicit §rotationPolicy: Always§ requests a fresh private key on reissuance. Controller status supplies the actual dates and readiness. The files contain no private key.',
runbook:'## Prerequisites\n\nExisting healthy cert-manager CRDs/controllers and permission to use a namespaced SelfSigned Issuer. If absent or disallowed, read the files and skip application.\n\n~~~powershell\nkubectl api-resources --api-group=cert-manager.io\nkubectl apply --dry-run=server -f topics/50-certificates-and-tls/optional/\nkubectl apply -f topics/50-certificates-and-tls/optional/\nkubectl -n k8s-learning-platform wait --for=condition=Ready certificate/catalog-lab --timeout=120s\nkubectl -n k8s-learning-platform describe certificate catalog-lab\nkubectl -n k8s-learning-platform get secret catalog-lab-tls -o custom-columns=NAME:.metadata.name,TYPE:.type\n~~~\n\nExpect Ready, validity/renewal information, and Secret type §kubernetes.io/tls§. This proves issuance/storage, not external trust or a live HTTPS connection.\n\n## Troubleshooting and cleanup\n\nInspect Issuer/CertificateRequest conditions for signing failures. Do not print Secret YAML or bypass TLS validation to claim success. Run cleanup only for resources created here:\n\n~~~powershell\nkubectl delete -f topics/50-certificates-and-tls/optional/ --ignore-not-found\nkubectl -n k8s-learning-platform delete secret catalog-lab-tls --ignore-not-found\n~~~\n\nExplicit Secret cleanup covers installations that do not garbage-collect the target Secret with the Certificate.'});
