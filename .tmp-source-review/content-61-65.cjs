const {topic}=require('./new40-content.cjs');
topic(61,{
summary:['Multiple clusters can separate failure domains, environments, or regions.','Each cluster has its own API, identities, policies, and operational state.','Cross-cluster traffic and data replication require explicit supporting systems.','Failover succeeds only when dependencies, capacity, and routing are ready.'],cue:'Separate control planes; connect only what is required.',plain:'Maya opens another branch so one building outage need not close every shop. Both branches still need catalog stock, staff access, and a way for customers to find the open branch.',story:'We inventory the local context and design a second Bookshop location without switching contexts or provisioning infrastructure.',
detail:'GitOps can distribute desired state to multiple targets, while fleet tools can centralize management. Neither automatically replicates databases or implements application failover. A shared identity provider, registry, region, or network can remain a common failure dependency.\n\nChoose cluster boundaries from recovery, latency, ownership, and residency requirements. More clusters add upgrade, observability, cost, and policy-management work. Record the exact context and namespace for each operation; a familiar object name may exist in several clusters.',source:'[Kubernetes multi-cluster SIG](https://github.com/kubernetes/community/tree/master/sig-multicluster), [cluster access organization](https://kubernetes.io/docs/concepts/configuration/organize-cluster-access-kubeconfig/)',
guide:'Read the [reliability namespace](../../manifests/00-namespace.yaml) and [Service](../../manifests/30-service.yaml). The namespace isolates names inside one cluster; creating another namespace is not a second cluster. Service DNS normally resolves within its cluster’s DNS domain.\n\nNo multi-cluster Service, federation controller, or remote kubeconfig is authored. A real design must specify discovery, routing, identity, data ownership/replication, and conflict handling. Reusing the same Deployment YAML in two places does not connect their data or traffic.',
runbook:'## Prerequisites\n\nLocal kubeconfig context names can be inspected without contacting every cluster. Do not dump raw kubeconfig credentials.\n\n~~~powershell\nkubectl config get-contexts -o name\nkubectl config current-context\nkubectl -n k8s-learning-reliability get deployment reliable-catalog\n~~~\n\n## Design exercise\n\nCreate a two-row branch inventory with context, region, namespace, data owner, routing owner, and recovery target. Mark whether identity, registry, DNS, and backup storage are shared dependencies. Define how a customer reaches the surviving branch and what write conflicts must be prevented.\n\nIf a second approved test cluster is already supplied, use explicit §--context§ on read commands instead of changing the global current context. A list of contexts is not a failover test.\n\n## Troubleshooting and cleanup\n\nDistinguish unreachable API, missing credentials, and missing resources in the selected cluster. No remote cluster or routing configuration is changed; no cleanup is needed.'});
topic(62,{
summary:['Hardening reduces unnecessary privileges and exposed attack surfaces.','Benchmarks describe checks whose applicability depends on the platform and version.','kube-bench evaluates accessible configuration against supported CIS benchmark checks.','Passing configuration checks does not prove complete runtime or application security.'],cue:'Choose the right benchmark; collect evidence; fix the actual gap.',plain:'Maya checks doors, staff keys, and the building’s inspection report. A passed building inspection does not prove every worker or transaction is safe.',story:'We review the catalog’s non-root settings, token exposure, network boundaries, and image identity. Node-level benchmark execution stays with the platform owner.',
detail:'A benchmark report must identify its version, target distribution, permissions, skipped tests, and manual checks. Managed control planes can hide settings that a tenant-side scanner cannot inspect. “Not checked” and “passed” are different results.\n\nRuntime detection, image vulnerability review, RBAC, admission, and network policy address other layers. Prioritize changes by exposure and evidence, then verify the workload still functions. Do not deploy a privileged host-scanning Job merely to make a report available for this chapter.',source:'[kube-bench project](https://github.com/aquasecurity/kube-bench), [Kubernetes security checklist](https://kubernetes.io/docs/concepts/security/security-checklist/)',
guide:'Read the [Deployment](../../manifests/20-deployment.yaml). §automountServiceAccountToken: false§ avoids an unnecessary API token. Pod UID/GID, seccomp, and container capability/escalation/filesystem settings reduce privileges. §team§ and §purpose§ labels support ownership but do not enforce authorization.\n\nThe image tag is explicit but mutable; no signature or vulnerability assertion is made. No NetworkPolicy exists in the base until a relevant exercise creates one. This walkthrough therefore produces a scoped review rather than a blanket “hardened” certification.',
runbook:'## Prerequisites\n\nRead permission for this namespace. A benchmark report must come from an approved scan appropriate to the platform.\n\n~~~powershell\nkubectl -n k8s-learning-reliability get deployment reliable-catalog -o yaml\nkubectl -n k8s-learning-reliability get serviceaccounts,roles,rolebindings,networkpolicies\nkubectl -n k8s-learning-reliability get pods -l app=reliable-catalog -o yaml\n~~~\n\n## Review result\n\nRecord each control, its evidence, and any gap. Separate authored settings from injected/live settings. For a provided kube-bench report, identify benchmark version and distinguish pass, fail, manual, and inaccessible checks. Map one actionable finding to a specific owner and verification step.\n\n## Troubleshooting and cleanup\n\nA denied read is a visibility limit, not automatic evidence of insecure configuration. No scanner is installed and no permissions are broadened, so there is nothing to remove.'});
topic(63,{
summary:['A mesh can authenticate and protect supported traffic between enrolled workloads.','Traffic policy can influence routing, retries, and timeouts.','Telemetry describes observed traffic but has coverage and sampling limits.','Encryption, authorization, and application identity must each be verified.'],cue:'Identify the peer, protect the path, measure the result.',plain:'Maya’s courier checks branch badges, protects packages, and records deliveries. A courier receipt still does not prove the package contents were correct or every road used the courier.',story:'The learner traces a hypothetical orders-to-catalog request and identifies what a real mesh would need to prove. The simple base website remains unmeshed by its authored configuration.',
detail:'Istio’s sidecar and ambient modes place traffic handling differently. Linkerd uses its own proxy/control-plane design and supported policy APIs. Version and configuration determine coverage; do not assume every Pod connection is encrypted or every protocol produces full request metrics.\n\nWeighted routing produces aggregate proportions, not an exact guarantee for every short sample. Retries can amplify failures and duplicate non-idempotent writes. Distributed tracing also depends on propagation and instrumentation choices; a proxy does not automatically reveal every internal application span.',source:'[Istio security](https://istio.io/latest/docs/concepts/security/), [Linkerd architecture](https://linkerd.io/2-edge/reference/architecture/)',
guide:'Read the [base Service](../../manifests/30-service.yaml) and [earlier mesh foundations](../../../04-bookshop-operations/topics/40-service-mesh-foundations/manifest-guide.md). The authored port and selector describe Kubernetes routing only.\n\nFor an existing enrolled workload, compare namespace/workload enrollment, source/destination identities, effective authentication/authorization policy, route rules, and telemetry. Mesh-specific custom resources are not interchangeable with ordinary NetworkPolicy or RBAC. No new mesh manifests are provided because this chapter does not select or install a mesh implementation.',
runbook:'## Prerequisites\n\nBase object reads need no mesh. Live mesh proof requires an already enrolled test workload, version-matched tooling, and permission from its owner.\n\n~~~powershell\nkubectl get namespace k8s-learning-reliability --show-labels\nkubectl -n k8s-learning-reliability get pods -l app=reliable-catalog -o yaml\nkubectl -n k8s-learning-reliability get service reliable-catalog -o yaml\n~~~\n\n## Evidence plan\n\nFor one request, record enrollment, negotiated protection, authenticated peer, authorization decision, route choice, response, and latency/error telemetry. Explain how a denied request and an unmeshed connection would look. A successful HTTP response alone proves none of the security claims.\n\nWhen no mesh exists, mark runtime mesh checks skipped and complete the trace design. Do not add injection labels or enable broad policies.\n\n## Cleanup\n\nThe exercise changes nothing; use the platform owner’s troubleshooting guide for gaps in existing telemetry.'});
topic(64,{
summary:['An upgrade changes several components with different compatibility constraints.','Supported version skew depends on the particular component pair.','Provider-supported sequencing and workload readiness determine a safe rollout.','Backups, rehearsal, and recovery decisions must precede production changes.'],cue:'Inventory, check compatibility, rehearse, progress in stages.',plain:'Maya renovates the shop’s electrical panel and counters in a planned order. Knowing that two parts are “close in age” is not enough to prove they work together.',story:'We build an upgrade checklist from actual client/node versions and the Bookshop API inventory. No node is cordoned, drained, or upgraded.',
detail:'There is no universal plus-or-minus-one rule for every Kubernetes component. For example, supported kubelet skew differs from kubectl skew and depends on version rules; kubelets must not be newer than the API server. Consult the current upstream policy and the distribution/provider’s narrower requirements for the exact source and target.\n\nCheck removed APIs, webhooks, CNI/CSI, runtimes, Operators, storage, and available replacement capacity. Deployment rollback is not a control-plane downgrade procedure. A rehearsal should test service behaviour, not just version strings.',source:'[Version skew policy](https://kubernetes.io/releases/version-skew-policy/), [kubeadm upgrade guidance](https://kubernetes.io/docs/tasks/administer-cluster/kubeadm/kubeadm-upgrade/)',
guide:'The [base Deployment](../../manifests/20-deployment.yaml) uses apps/v1; its readiness and rollingUpdate settings describe application behaviour, not cluster upgrade order. The [Service](../../manifests/30-service.yaml) and ConfigMap use core v1 APIs.\n\nInventory node kubelet/runtime versions, API server version, client version, and add-on/CRD versions separately. A PDB constrains supported eviction requests but does not prevent all upgrade-related outages. No upgrade command or fake universal version matrix is embedded in these manifests.',
runbook:'## Prerequisites\n\nRead permission for version and node information. Choose a proposed source/target version pair using the provider’s supported path.\n\n~~~powershell\nkubectl version -o yaml\nkubectl get nodes -o custom-columns=NAME:.metadata.name,KUBELET:.status.nodeInfo.kubeletVersion,RUNTIME:.status.nodeInfo.containerRuntimeVersion\nkubectl -n k8s-learning-reliability get deploy,pods,svc,pdb\n~~~\n\n## Prepare the reviewable plan\n\nRecord component compatibility, deprecated APIs, backup/restore evidence, upgrade order, workload checks, stop conditions, and recovery owner. Include capacity and disruption-budget checks before a planned node maintenance step. Determine which operations are provider-managed and which are yours.\n\n## Expected result and cleanup\n\nA complete plan distinguishes a validated compatibility rule from an assumption and names the staging evidence still needed. No upgrades are performed here. A failed version read is a visibility/connectivity problem; no cleanup is required.'});
topic(65,{
summary:['Recovery requires matching each data type to a tested restore method.','An etcd snapshot protects API state rather than every application volume.','Velero and storage backup methods have scope and consistency requirements.','A restore drill must verify recovered application behaviour and recovery time.'],cue:'A backup exists; a restore proves it is useful.',plain:'Maya keeps the counter plans, the stock records, and the keys separately. Rebuilding the counters is useful, but does not prove the missing stock records were recovered.',story:'We rebuild the disposable static catalog in a separate recovery namespace from authored files. This is a configuration-rebuild drill with an HTTP check, explicitly smaller than full disaster recovery.',
detail:'Define recovery point and time objectives from acceptable data loss and downtime. Database consistency, external services, credentials, encryption keys, storage drivers, and network routing can all affect recovery. Git contains only what was committed, and ordinary API exports are not a complete backup system.\n\nModern etcd recovery uses version-appropriate snapshot/restore tools; Kubernetes watch consumers can require revision-bump considerations during restoration. Velero’s object and volume coverage depends on configuration and supported plugins/methods. Never restore into a live control plane as an application lesson.',source:'[etcd recovery](https://etcd.io/docs/v3.6/op-guide/recovery/), [Velero documentation](https://velero.io/docs/main/)',
guide:'Files: [restore base](restore/base/kustomization.yaml), [page](restore/base/10-page.yaml), [Deployment](restore/base/20-deployment.yaml), [Service](restore/base/30-service.yaml), [practice overlay](restore/practice/kustomization.yaml), and [recovery namespace](restore/practice/namespace.yaml).\n\nThe base copies only authored, non-secret catalog configuration. The overlay sets §namespace: k8s-learning-recovery§ and includes its Namespace resource. Kustomize rewrites namespaced identities while preserving matching selectors and ConfigMap references.\n\nNo PVC, external database, live Secret, or control-plane snapshot is included. Successful rendering proves configuration structure; the runbook’s request verifies the rebuilt static service.',
runbook:'## Prerequisites\n\nA fresh recovery namespace, spare capacity, image access, and permission for the four resource types. Inspect an existing namespace before using it.\n\n~~~powershell\nkubectl get namespace k8s-learning-recovery\nkubectl kustomize topics/65-disaster-recovery/restore/practice\nkubectl apply -k topics/65-disaster-recovery/restore/practice\nkubectl -n k8s-learning-recovery rollout status deployment/reliable-catalog --timeout=120s\nkubectl -n k8s-learning-recovery exec deployment/reliable-catalog -- wget -T 3 -qO- http://reliable-catalog\n~~~\n\nExpect the reliability page from the separate namespace. Record elapsed time and dependencies. This proves configuration rebuild only; mark volume/database/etcd recovery untested.\n\n## Recovery worksheet\n\nFor a real shop, list each state store, backup location, access/key dependency, consistency method, recovery target, and restore verification. A backup job reporting success is insufficient evidence by itself.\n\n## Cleanup\n\nAfter confirming that the namespace still contains only the drill:\n\n~~~powershell\nkubectl -n k8s-learning-recovery get deploy,pods,svc,cm,pvc\nkubectl delete namespace k8s-learning-recovery\n~~~'});
