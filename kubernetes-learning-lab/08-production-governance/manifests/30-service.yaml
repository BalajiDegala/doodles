apiVersion: v1
kind: Service
metadata:
  name: governance-catalog
  namespace: k8s-learning-governance
spec:
  type: ClusterIP
  selector:
    app: governance-catalog
  ports:
    - name: http
      port: 80
      targetPort: http
