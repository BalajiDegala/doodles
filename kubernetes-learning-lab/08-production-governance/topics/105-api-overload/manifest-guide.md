# 105. API server overloaded: slow kubectl and timeouts: manifest walkthrough

The base namespace is a small unit for a scoped read. Record request verb, resource, selector, timeout, duration, and timestamp. No API server tuning or load generator is provided. The [scale lesson](../../../06-bookshop-reliability/topics/68-large-clusters/lesson.md) separates control-plane demand from worker utilization.

The [shared manifest guide](../../manifest-guide.md) explains the catalog's selectors, port, resources, and security controls. Continue with the [runbook](runbook.md).
