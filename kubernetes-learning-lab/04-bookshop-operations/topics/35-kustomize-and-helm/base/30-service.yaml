apiVersion: v1
kind: Service
metadata:
  name: bookshop-custom
  namespace: k8s-learning-operations
spec:
  selector:
    app: bookshop-custom
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
