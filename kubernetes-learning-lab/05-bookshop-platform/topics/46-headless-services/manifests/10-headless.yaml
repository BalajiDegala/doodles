apiVersion: v1
kind: Service
metadata:
  name: catalog-peers
  namespace: k8s-learning-platform
spec:
  clusterIP: None
  selector:
    app: platform-catalog
  ports:
    - name: http
      port: 80
      targetPort: http
