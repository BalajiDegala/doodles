apiVersion: v1
kind: Service
metadata:
  name: canary-web
  namespace: k8s-learning-canary
spec:
  type: ClusterIP
  selector:
    app: canary-web
  ports:
    - name: http
      port: 80
      targetPort: http
