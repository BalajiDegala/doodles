apiVersion: v1
kind: Service
metadata:
  name: incident-catalog
  namespace: k8s-learning-incidents
spec:
  type: ClusterIP
  selector:
    app: incident-catalog
  ports:
    - name: http
      port: 80
      targetPort: http
