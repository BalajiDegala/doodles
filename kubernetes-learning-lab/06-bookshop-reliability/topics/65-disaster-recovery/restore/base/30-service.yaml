apiVersion: v1
kind: Service
metadata:
  name: reliable-catalog
  namespace: k8s-learning-reliability
spec:
  type: ClusterIP
  selector:
    app: reliable-catalog
  ports:
    - name: http
      port: 80
      targetPort: http
